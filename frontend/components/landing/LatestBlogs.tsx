import Link from "next/link";
import { ArrowUpRight, Search } from "lucide-react";
import type { Blog } from "@/types/blog.types";
import ReaderBlogCard from "@/components/reader/readerBlogCard";

export default function LatestBlogs({ blogs, loading, query, onQueryChange }: { blogs: Blog[]; loading: boolean; query: string; onQueryChange: (value: string) => void }) {
  return <section id="explore" className="mx-auto max-w-7xl scroll-mt-20 px-5 py-14 sm:px-8 lg:px-10 lg:py-20"><div className="mb-8 flex flex-col gap-5 sm:flex-row sm:items-end sm:justify-between"><div><p className="text-sm font-semibold text-violet-600 dark:text-violet-400">Fresh from the community</p><h2 className="mt-1 text-3xl font-bold tracking-tight text-slate-900 dark:text-white">Latest blogs</h2></div><div className="flex flex-col gap-4 sm:items-end"><div className="relative w-full sm:w-64"><Search className="absolute left-3 top-1/2 h-4 w-4 -translate-y-1/2 text-slate-400" /><input value={query} onChange={(event) => onQueryChange(event.target.value)} placeholder="Search published blogs" className="landing-search-input w-full rounded-xl border border-slate-200 bg-white py-2.5 pl-9 pr-3 text-sm text-slate-900 outline-none placeholder:text-slate-500 focus:border-violet-400 dark:border-slate-700 dark:bg-slate-900 dark:text-white dark:placeholder:text-slate-400 dark:focus:border-violet-500" /></div><Link href="/blogs" className="inline-flex items-center gap-2 text-sm font-semibold text-violet-600 hover:text-violet-500 dark:text-violet-400">View all blogs <ArrowUpRight className="h-4 w-4" /></Link></div></div>{loading ? <div className="grid gap-5 sm:grid-cols-2 lg:grid-cols-3">{[1, 2, 3].map((item) => <div key={item} className="h-80 animate-pulse rounded-2xl border border-slate-200 bg-slate-100 dark:border-slate-800 dark:bg-slate-900" />)}</div> : blogs.length ? <div className="grid gap-5 sm:grid-cols-2 lg:grid-cols-3">{blogs.map((blog) => <ReaderBlogCard key={blog._id} blog={blog} />)}</div> : <div className="rounded-2xl border border-dashed border-slate-300 bg-white p-12 text-center text-sm text-slate-500 dark:border-slate-700 dark:bg-slate-900 dark:text-slate-400">No published blogs found.</div>}</section>;
}
